# Normalize line endings to LF in the repo and on checkout. Critical for
# docker-entrypoint.sh: a CRLF shebang breaks /bin/sh inside the Linux container.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
